<section class="engineering-hero" aria-labelledby="engineering-hero-title">
    <div class="engineering-hero__inner">
        <p class="engineering-hero__eyebrow">SPACESCALE ENGINEERING BLOG</p>
        <h1 id="engineering-hero-title" class="engineering-hero__title">
            Latest <span class="engineering-hero__highlight">updates</span> from the
            engineering team
        </h1>
    </div>
</section>

<style>
    .engineering-hero {
        position: relative;
        display: flex;
        width: 100%;
        min-height: 174px;
        align-items: flex-start;
        padding: 70px var(--site-padding-x) 20px;
        background: var(--color-bg-primary);
    }

    .engineering-hero::after {
        position: absolute;
        right: 0;
        bottom: 0;
        left: 0;
        height: 1px;
        background-image: repeating-linear-gradient(
            to right,
            #262b33 0,
            #262b33 6px,
            transparent 6px,
            transparent 12px
        );
        content: "";
    }

    .engineering-hero__inner {
        position: relative;
        display: flex;
        width: 100%;
        flex-direction: column;
        gap: 16px;
        overflow: clip;
    }

    .engineering-hero__eyebrow,
    .engineering-hero__title {
        margin: 0;
    }

    .engineering-hero__eyebrow {
        color: #8c94a1;
        font-family: var(--font-body);
        font-size: 13px;
        font-weight: 500;
        line-height: 16px;
        letter-spacing: 1.04px;
        white-space: nowrap;
    }

    .engineering-hero__title {
        width: 900px;
        max-width: 100%;
        color: #f5f7fa;
        font-family: var(--font-display);
        font-size: 44px;
        font-weight: 400;
        line-height: 52px;
        letter-spacing: -0.66px;
    }

    .engineering-hero__highlight {
        position: relative;
        z-index: 0;
        display: inline-block;
        color: #040406;
    }

    .engineering-hero__highlight::before {
        position: absolute;
        z-index: -1;
        top: -2px;
        left: 0;
        width: 100%;
        height: 59px;
        background: #66d9b2;
        content: "";
    }

    @media (max-width: 1180px) {
        .engineering-hero__title {
            font-size: 38px;
            line-height: 46px;
            letter-spacing: -0.5px;
        }

        .engineering-hero__highlight::before {
            height: 53px;
        }
    }

    @media (max-width: 900px) {
        .engineering-hero {
            min-height: 0;
            padding-top: 56px;
        }

        .engineering-hero__title {
            font-size: clamp(28px, 5vw, 38px);
            line-height: 1.18;
            letter-spacing: -0.48px;
        }

        .engineering-hero__highlight::before {
            height: 1.34em;
        }
    }

    @media (max-width: 640px) {
        .engineering-hero {
            padding-top: 48px;
        }

        .engineering-hero__eyebrow {
            font-size: 12px;
            line-height: 15px;
            letter-spacing: 0.96px;
        }

        .engineering-hero__title {
            font-size: 28px;
            line-height: 35px;
            letter-spacing: -0.32px;
        }

        .engineering-hero__highlight::before {
            height: 44px;
        }
    }
</style>
