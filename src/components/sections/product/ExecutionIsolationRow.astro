<div class="execution-isolation-copy">
    <h2>Absolute Execution Isolation</h2>
    <p>
        Simultaneous multithreading is disabled on all our compute. Every workload runs
        inside a hardened microVM. Dedicated reservations add physical core
        assignment and CPU pinning for workloads that need more predictable
        execution.
    </p>
</div>

<div class="execution-isolation-visual" aria-hidden="true">
    <!-- Keep one decorative visual node here. Separate desktop/mobile images once
    rendered together during resize and leaked into the next product row. -->
    <div class="execution-isolation-visual__asset"></div>
</div>

<style>
    .execution-isolation-copy {
        --execution-isolation-copy-tablet-top: 72px;

        position: absolute;
        top: 81px;
        left: 96px;
        width: 389px;
    }

    .execution-isolation-copy h2 {
        margin: 0;
        color: #f5f7fa;
        font-family: var(--font-body);
        font-size: 28px;
        font-weight: 400;
        line-height: 36px;
        letter-spacing: -0.28px;
    }

    .execution-isolation-copy p {
        margin: 16px 0 0;
        color: #8c94a1;
        font-family: var(--font-body);
        font-size: 15px;
        font-weight: 400;
        line-height: 24px;
    }

    .execution-isolation-visual {
        --execution-isolation-visual-tablet-top: 68px;
        --execution-isolation-visual-tablet-left: 410px;
        --execution-isolation-visual-tablet-max-width: 650px;

        position: absolute;
        top: 63px;
        right: 0;
        width: min(770px, calc(100% - 540px));
        height: auto;
        aspect-ratio: 770 / 476;
        overflow: clip;
        user-select: none;
    }

    .execution-isolation-visual__asset {
        width: 100%;
        height: 100%;
        background-image: url("/assets/product/execution-isolation.svg");
        background-position: center;
        background-repeat: no-repeat;
        background-size: contain;
    }

    @media (max-width: 1180px) {
        .execution-isolation-copy {
            top: var(--execution-isolation-copy-tablet-top);
            left: var(--product-row-tablet-copy-left);
            width: var(--product-row-tablet-copy-width);
        }

        .execution-isolation-visual {
            top: var(--execution-isolation-visual-tablet-top);
            right: 0;
            left: auto;
            width: min(
                var(--execution-isolation-visual-tablet-max-width),
                calc(100% - var(--execution-isolation-visual-tablet-left))
            );
            height: auto;
        }

        .execution-isolation-visual__asset {
            width: 100%;
            aspect-ratio: 770 / 476;
        }
    }

    @media (max-width: 900px) {
        .execution-isolation-copy {
            position: relative;
            top: auto;
            left: auto;
            width: 100%;
            max-width: var(--product-row-mobile-copy-max-width);
        }

        .execution-isolation-visual {
            position: relative;
            top: auto;
            right: auto;
            left: auto;
            width: 100%;
            height: auto;
            margin-top: var(--product-row-mobile-visual-gap);
            overflow: visible;
        }

        .execution-isolation-visual__asset {
            width: 100%;
            max-width: 770px;
        }
    }

    @media (max-width: 640px) {
        .execution-isolation-copy h2 {
            font-size: var(--product-row-mobile-heading-size);
            line-height: var(--product-row-mobile-heading-line-height);
            letter-spacing: 0;
        }

        .execution-isolation-copy p {
            font-size: var(--product-row-mobile-body-size);
            line-height: var(--product-row-mobile-body-line-height);
        }

        .execution-isolation-visual {
            margin-top: var(--product-row-mobile-visual-gap-tight);
        }

        .execution-isolation-visual__asset {
            width: 224px;
            max-width: none;
            height: 256px;
            margin: 0 auto;
            background-image: url("/assets/product/execution-isolation-mobile.svg");
        }
    }
</style>
