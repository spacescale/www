<div class="performance-copy">
    <h2>Performance at its peak</h2>
    <p>
        Fast starts. Direct routing. Predictable compute. SpaceScale is built to
        ignite workloads in milliseconds and keep traffic moving close to the metal.
    </p>
</div>

<div class="performance-visual" aria-hidden="true">
    <div class="performance-visual__asset"></div>
</div>

<style>
    .performance-copy {
        --performance-copy-tablet-top: 88px;
        --performance-copy-mobile-max-width: 430px;

        position: absolute;
        top: 96px;
        left: 95px;
        width: 389px;
    }

    .performance-copy h2 {
        margin: 0;
        color: #f5f7fa;
        font-family: var(--font-body);
        font-size: 28px;
        font-weight: 400;
        line-height: 28px;
        letter-spacing: -0.28px;
    }

    .performance-copy p {
        margin: 16px 0 0;
        color: #8c94a1;
        font-family: var(--font-body);
        font-size: 15px;
        font-weight: 400;
        line-height: 24px;
    }

    .performance-visual {
        --performance-visual-tablet-top: 0px;
        --performance-visual-tablet-left: 410px;
        --performance-visual-tablet-max-width: 672px;

        position: absolute;
        top: 0;
        right: 0;
        width: min(672px, calc(100% - 520px));
        height: auto;
        aspect-ratio: 672 / 580;
        overflow: clip;
        user-select: none;
    }

    .performance-visual__asset {
        width: 100%;
        height: 100%;
        background-image: url("/assets/product/performance-latency-graph.svg");
        background-position: center;
        background-repeat: no-repeat;
        background-size: contain;
    }

    @media (max-width: 1180px) {
        .performance-copy {
            top: var(--performance-copy-tablet-top);
            left: var(--product-row-tablet-copy-left);
            width: var(--product-row-tablet-copy-width);
        }

        .performance-visual {
            top: var(--performance-visual-tablet-top);
            right: 0;
            left: auto;
            width: min(
                var(--performance-visual-tablet-max-width),
                calc(100% - var(--performance-visual-tablet-left))
            );
            height: auto;
        }

        .performance-visual__asset {
            width: 100%;
            max-width: var(--performance-visual-tablet-max-width);
            aspect-ratio: 672 / 580;
        }
    }

    @media (max-width: 900px) {
        .performance-copy {
            position: relative;
            top: auto;
            left: auto;
            width: 100%;
            max-width: var(--performance-copy-mobile-max-width);
        }

        .performance-visual {
            position: relative;
            top: auto;
            right: auto;
            left: auto;
            width: 100%;
            height: auto;
            margin-top: var(--product-row-mobile-visual-gap);
            overflow: visible;
        }

        .performance-visual__asset {
            width: 100%;
            max-width: 672px;
        }
    }

    @media (max-width: 640px) {
        .performance-copy h2 {
            font-size: var(--product-row-mobile-heading-size);
            line-height: var(--product-row-mobile-heading-line-height);
            letter-spacing: 0;
        }

        .performance-copy p {
            font-size: var(--product-row-mobile-body-size);
            line-height: var(--product-row-mobile-body-line-height);
        }

        .performance-visual {
            margin-top: var(--product-row-mobile-visual-gap-tight);
        }
    }
</style>
