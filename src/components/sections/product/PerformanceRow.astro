<div class="performance-copy">
    <h2>Performance at its peak</h2>
    <p>
        Fast starts. Direct routing. Predictable compute. SpaceScale is built to
        ignite workloads in milliseconds and keep traffic moving close to the metal.
    </p>
</div>

<style>
    .performance-copy {
        --performance-copy-tablet-top: 104px;
        --performance-copy-mobile-max-width: 430px;

        position: absolute;
        top: 127px;
        left: 95px;
        width: 389px;
    }

    .performance-copy h2 {
        margin: 0;
        color: #f5f7fa;
        font-family: var(--font-body);
        font-size: 28px;
        font-weight: 400;
        line-height: 28px;
        letter-spacing: -0.28px;
    }

    .performance-copy p {
        margin: 16px 0 0;
        color: #8c94a1;
        font-family: var(--font-body);
        font-size: 15px;
        font-weight: 400;
        line-height: 24px;
    }

    @media (max-width: 1180px) {
        .performance-copy {
            top: var(--performance-copy-tablet-top);
            left: var(--product-row-tablet-copy-left);
            width: var(--product-row-tablet-copy-width);
        }
    }

    @media (max-width: 900px) {
        .performance-copy {
            position: relative;
            top: auto;
            left: auto;
            width: 100%;
            max-width: var(--performance-copy-mobile-max-width);
        }

    }

    @media (max-width: 640px) {
        .performance-copy h2 {
            font-size: var(--product-row-mobile-heading-size);
            line-height: var(--product-row-mobile-heading-line-height);
            letter-spacing: 0;
        }

        .performance-copy p {
            font-size: var(--product-row-mobile-body-size);
            line-height: var(--product-row-mobile-body-line-height);
        }
    }
</style>
