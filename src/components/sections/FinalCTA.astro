<section class="final-cta" aria-labelledby="final-cta-title">
    <div class="final-cta__container">
        <p class="final-cta__eyebrow">ALMOST READY...</p>
        <h2 id="final-cta-title" class="final-cta__title">We are in active development</h2>
        <p class="final-cta__copy">
            Read our engineering blog to learn more about our design decisions,
            architecture tradeoffs, and the systems work behind SpaceScale as we
            build toward the first public runtime.
        </p>
        <a class="final-cta__button" href="/blog">Read how we're building it</a>
    </div>
</section>

<style>
    .final-cta {
        display: flex;
        width: 100%;
        align-items: center;
        justify-content: center;
        position: relative;
        z-index: 0;
        height: 523px;
        padding: 120px var(--site-padding-x);
        overflow: clip;
        background: var(--color-bg-primary);
    }

    .final-cta__container {
        display: flex;
        width: 900px;
        max-width: 100%;
        flex-direction: column;
        align-items: center;
        gap: 32px;
        text-align: center;
    }

    .final-cta__eyebrow,
    .final-cta__title,
    .final-cta__copy {
        margin: 0;
    }

    .final-cta__eyebrow {
        color: #66d9b2;
        font-family: "Geist Mono", var(--font-mono);
        font-size: 13px;
        font-weight: 500;
        line-height: normal;
        letter-spacing: 1.04px;
        white-space: nowrap;
    }

    .final-cta__title {
        width: 900px;
        max-width: 100%;
        color: #f5f7fa;
        font-family: var(--font-display);
        font-size: 56px;
        font-weight: 400;
        line-height: 64px;
        letter-spacing: -1.12px;
    }

    .final-cta__copy {
        width: 786px;
        max-width: 100%;
        color: #8c94a1;
        font-family: var(--font-body);
        font-size: 18px;
        font-weight: 400;
        line-height: 28px;
    }

    @media (max-width: 900px) {
        .final-cta {
            height: auto;
            padding: 80px var(--site-padding-x);
        }

        .final-cta__title {
            font-size: 40px;
            line-height: 48px;
            letter-spacing: -0.8px;
        }

        .final-cta__copy {
            font-size: 16px;
            line-height: 26px;
        }

    }

    @media (max-width: 640px) {
        .final-cta {
            padding: 64px var(--site-padding-x);
        }

        .final-cta__container {
            gap: 24px;
        }

        .final-cta__title {
            font-size: 32px;
            line-height: 38px;
            letter-spacing: -0.4px;
        }

        .final-cta__copy {
            font-size: 15px;
            line-height: 23px;
        }

    }

    .final-cta__button {
        display: inline-flex;
        min-width: 250px;
        height: 51px;
        align-items: center;
        justify-content: center;
        padding: 16px 24px;
        border: 1px solid #262b33;
        color: #f5f7fa;
        font-family: var(--font-body);
        font-size: 16px;
        font-weight: 600;
        line-height: normal;
        white-space: nowrap;
        transition:
            border-color var(--motion-fast),
            background-color var(--motion-fast),
            color var(--motion-fast);
    }

    .final-cta__button:hover {
        border-color: #343a45;
        color: rgba(245, 247, 250, 0.86);
    }
</style>
