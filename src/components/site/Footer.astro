---
/*
  Footer.astro is the shared footer for marketing pages.

  We keep footer link data in arrays so the markup stays clean.
  Later, if links change, we edit the arrays instead of rewriting HTML.
*/

const footerGroups = [
    {
        title: "Resources",
        links: ["Documentation", "Changelog", "Partnerships"],
    },
    {
        title: "Blog",
        links: ["Engineering", "Tutorials"],
    },
    {
        title: "Company",
        links: ["Principles", "Careers"],
    },
];

const legalLinks = ["Privacy", "Terms", "Security"];
const currentYear = new Date().getFullYear();

const socialLinks = [
    {
        label: "SpaceScale on LinkedIn",
        href: "https://www.linkedin.com/company/spacescale",
        icon: "/assets/social/linkedin.svg",
    },
    {
        label: "SpaceScale on GitHub",
        href: "https://github.com/spacescale",
        icon: "/assets/social/github.svg",
    },
    {
        label: "SpaceScale on YouTube",
        href: "#",
        icon: "/assets/social/youtube.svg",
    },
] as const;
---

<footer class="site-footer">
    <div class="footer-inner">
        <section class="footer-brand" aria-label="SpaceScale">
            <h2>Infrastructure for<br />Serious Workloads</h2>
            <div class="status-badge" aria-label="System status">
                <span aria-hidden="true"></span>
                All systems operational
            </div>

            <div class="social-links" aria-label="Social links">
                {
                    socialLinks.map((link) => (
                        <a href={link.href} aria-label={link.label}>
                            <span
                                class="site-social-icon"
                                style={`--site-social-icon: url(${link.icon})`}
                                aria-hidden="true"
                            />
                        </a>
                    ))
                }
            </div>
        </section>

        <nav class="footer-links" aria-label="Footer navigation">
            {
                footerGroups.map((group) => (
                    <section>
                        <h3>{group.title}</h3>

                        <ul>
                            {group.links.map((link) => (
                                <li>
                                    <a href="#">{link}</a>
                                </li>
                            ))}
                        </ul>
                    </section>
                ))
            }
        </nav>
    </div>

    <div class="footer-divider" aria-hidden="true"></div>

    <div class="footer-bottom">
        <p>© <span data-footer-year>{currentYear}</span> SpaceScale Systems Inc.</p>
        <nav aria-label="Legal links">
            {
                legalLinks.map((link) => (
                    <a href="#">{link}</a>
                ))
            }
        </nav>
    </div>
</footer>

<script>
    const footerYear = document.querySelector("[data-footer-year]");

    if (footerYear) {
        footerYear.textContent = String(new Date().getFullYear());
    }
</script>
